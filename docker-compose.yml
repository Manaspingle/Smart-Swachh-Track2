services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: swachh
      POSTGRES_PASSWORD: swachh
      POSTGRES_DB: swachhtrack
    ports:
      - "5432:5432"
    volumes:
      - swachhtrack_pgdata:/var/lib/postgresql/data

volumes:
  swachhtrack_pgdata:

